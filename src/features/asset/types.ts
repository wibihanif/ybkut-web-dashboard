export interface ScrapProduct {
  name: string;
  runningDepreciation: string;
}

export interface DepreciationItem {
  name: string;
  runningDepreciation: string;
  bookValue: string;
  valueResidual: string;
}
